apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cloudflare-tunnel
  namespace: flux-system
spec:
  interval: 10m
  path: ./kubernetes/apps/staging/cloudflare-tunnel
  prune: true
  sourceRef:
    kind: GitRepository
    name: aks-system
    namespace: flux-system
