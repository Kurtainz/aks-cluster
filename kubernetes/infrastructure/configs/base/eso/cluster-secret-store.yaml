apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: azure-keyvault
spec:
  provider:
    azurekv:
      tenantId: ${TENANT_ID}
      authType: WorkloadIdentity
      vaultUrl: "https://kv-aks-cluster-staging.vault.azure.net"
      serviceAccountRef:
        name: external-secrets-sa
        namespace: external-secrets
